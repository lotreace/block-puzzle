{
  "name": "block-puzzle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://lotreace.github.io/block-puzzle",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/block-puzzle/",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.3",
    "gh-pages": "^6.3.0",
    "jsdom": "^27.0.1",
    "vite": "^7.1.7",
    "vitest": "^4.0.3"
  }
}
